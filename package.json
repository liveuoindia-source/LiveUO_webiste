{
  "name": "liveuo-website",
  "version": "1.0.0",
  "private": true,
  "description": "LiVEUO — static marketing site plus an Express/SQLite Downloads Manager backend.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node scripts/build.js && node scripts/check-placeholders.js",
    "check": "node scripts/build.js --check && node scripts/check-placeholders.js",
    "test": "npm run check"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.2",
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.1",
    "express-session": "^1.19.0",
    "motion": "^12.42.2",
    "multer": "^2.2.0",
    "nodemailer": "^6.10.1"
  }
}
